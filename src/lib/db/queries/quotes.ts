// TODO: Implement quote queries
// - getSavedQuotesByEntryId (returns all quotes saved for a specific entry)
// - getUserSavedQuotes (returns all quotes saved by a specific user)
// - isQuoteSaved (returns true if a quote is saved by a user for a specific entry)
